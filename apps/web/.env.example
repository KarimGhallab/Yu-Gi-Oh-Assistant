# Base URL the client calls for the API. Leave it empty to use the same origin,
# which both the Vite dev proxy and a reverse proxy serve. Set it when the
# client is hosted separately from the server.
# VITE_API_BASE_URL=https://api.example.com
