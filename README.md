# Template for Node/TS monorepos

This repository contains a template running with node 20 and typescript 5.5.
It has two share library projects in the `libs` folder and one console application in the `apps` folder.
